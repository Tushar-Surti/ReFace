# Base Face Models

Place your base face OBJ model here as `base_face.obj`.

The model should be:
- Oriented with Y-forward, Z-up
- Scaled to approximately head-sized proportions
- Preferably with UV coordinates and shape keys (blend shapes)

If no model is present, the app generates a procedural parametric head mesh.
    